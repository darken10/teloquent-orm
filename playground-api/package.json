{
  "name": "teloquent-playground-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "API REST Express de démonstration consommant Teloquent via un lien local (file:).",
  "scripts": {
    "migrate": "tsx src/db.ts migrate",
    "seed": "tsx src/db.ts seed",
    "fresh": "tsx src/db.ts fresh",
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "smoke": "tsx src/smoke.ts"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "express": "^4.19.2",
    "teloquent": "file:.."
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
